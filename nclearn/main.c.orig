#include <stdio.h>

int main(int argc, char *argv[])
{
	printf("Hello C-Free!\n");
	int a[] = {
		1,2,3,4
	};
	int *p = a;
	printf("%d\n",*(p+1) );
	return 0;
}

int sum(int *arr,int size){
	int sum = 0;
	int *ptr = arr;
	while(ptr <= arr[size-1]){
		sum += *ptr;
		ptr++;
	}
	}